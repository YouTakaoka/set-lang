[必要最小限の機能に絞る]
・いずれにせよseteqは自然数判定に必要
・pairはないと算術が成り立たないので必要（x->{x}で代用可）
・unionはsuccを定義するのに必要（ただし二項演算でOK）

[その他アイデア]
・扱える集合の階数を有限にする
・遅延評価
・判定不可能はものはエラー

========= 以下ボツアイデア ==========
この場合は、xを有限階に制限したものをx_nとして
union {setminus(x_n,y) | n in N}
とすればよい。

midset(x,y)も同様に、yが有限階であれば問題はないから、
union {midset(x,y_n) | n in N}
とすればよい。
------------------------------------
これだと無限階の要素をもつ集合に対応できない．
たとえば{{}, N}とか．
=====================================